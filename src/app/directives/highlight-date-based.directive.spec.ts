import { HighlightDateBasedDirective } from './highlight-date-based.directive';

describe('HighlightDateBasedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDateBasedDirective();
    expect(directive).toBeTruthy();
  });
});
